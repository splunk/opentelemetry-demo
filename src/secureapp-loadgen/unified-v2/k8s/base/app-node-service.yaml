apiVersion: v1
kind: Service
metadata:
  name: app-node
  labels:
    app.kubernetes.io/name: app-node
    app.kubernetes.io/language: node
    app.kubernetes.io/part-of: unified-secureapp
spec:
  type: ClusterIP
  selector:
    app.kubernetes.io/name: app-node
  ports:
    - name: http
      port: 8080
      targetPort: http
